{
  "name": "agentmeter",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Локальный счётчик токенов для кодинг-агентов",
  "license": "MIT",
  "engines": {
    "node": ">=22.12.0"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc --build && npm run build:bundles",
    "build:bundles": "npm run build:preload -w @agentmeter/desktop && npm run build:web -w @agentmeter/desktop",
    "check": "npm run lint && npm run build && npm test",
    "clean": "tsc --build --clean",
    "cli": "node --experimental-strip-types apps/cli/src/main.ts",
    "fixtures": "node --experimental-strip-types scripts/make-fixtures.ts",
    "fixtures:check": "node --experimental-strip-types scripts/check-fixtures.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --build --force"
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "@types/node": "^22.10.5",
    "eslint": "^9.18.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.20.0",
    "vitest": "^3.0.2"
  }
}
